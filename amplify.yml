version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for static HTML site"
    build:
      commands:
        - echo "Skipping build"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []