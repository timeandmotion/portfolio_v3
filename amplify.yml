version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for static HTML site"
    build:
      commands:
        - echo "Skipping build - deploying static files"
  artifacts:
    # All files are in the root directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .git/**/*